import { Rubik } from 'next/font/google';
import './globals.css';

const rubik = Rubik({
  subsets: ['latin'], // Subset Latin
  weight: ['300', '400', '500', '700'], // Berat font
  style: ['normal', 'italic'], // Gaya font
  display: 'swap', // Opsi display (default: "swap")
});
